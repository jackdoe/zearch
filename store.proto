syntax = "proto3";

package main;
message StoredPostingsList {
        string term = 1
        int32 begin = 2;
        int32 len = 3;
        repeated int32 ids = 2;
}

message StoredSegment {
        repeated string documents = 1;
        repeated StoredPostingsList postings = 2;
}
